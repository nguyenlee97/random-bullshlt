import * as React from 'react'
import { LandingFinalCta } from 'agent-frontend'

export const Default = () => (
  <div style={{ fontFamily: 'Inter, system-ui, sans-serif' }}>
    <LandingFinalCta onEnterAgent={() => {}} />
  </div>
)
