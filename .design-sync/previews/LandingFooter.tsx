import * as React from 'react'
import { LandingFooter } from 'agent-frontend'

export const Default = () => (
  <div style={{ fontFamily: 'Inter, system-ui, sans-serif' }}>
    <LandingFooter onEnterAgent={() => {}} />
  </div>
)
